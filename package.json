{
  "name": "collabboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "client": "npm run dev --prefix client",
    "server": "node server/server.js",
    "ai-server": "cd ai-server && venv\\Scripts\\python.exe main.py",
    "dev": "concurrently --names \"SERVER,CLIENT\" -c \"blue,green\" \"npm run server\" \"npm run client\"",
    "dev:all": "concurrently --names \"SERVER,CLIENT,AI\" -c \"blue,green,magenta\" \"npm run server\" \"npm run client\" \"npm run ai-server\"",
    "install:all": "npm install --prefix client && npm install --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
